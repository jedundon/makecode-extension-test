namespace gabe {
    //% block
    export function passwordchecker(password: string) {
        actual_password = "6099"
        return convertToText(password) == actual_password
    }
    let actual_password = ""
}
